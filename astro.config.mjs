// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://vazkeft.com',
  trailingSlash: 'ignore',
  devToolbar: { enabled: false },
});
